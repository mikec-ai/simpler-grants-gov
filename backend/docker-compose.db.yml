services:
  grants-db:
    image: postgres:17.5-alpine
    command: postgres -c "log_lock_waits=on" -N 1000 -c "fsync=off"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret123
      POSTGRES_MULTIPLE_DATABASES: "app,app:analytics,analytics"
    ports:
      - "${DB_HOST_PORT:-5432}:5432"
    volumes:
      - grantsdbdata:/var/lib/postgresql/data
      - ./scripts/init-dbs.sh:/docker-entrypoint-initdb.d/init-dbs.sh
    networks:
      - default
      - local_db

networks:
  local_db:
    name: ${LOCAL_DB_NETWORK_NAME:-local_db}

volumes:
  grantsdbdata:
